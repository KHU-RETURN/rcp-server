// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/KHU-RETURN/rcp-server/ent/keypair"
	"github.com/google/uuid"
)

// KeyPair is the model entity for the KeyPair schema.
type KeyPair struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// OpenstackName holds the value of the "openstack_name" field.
	OpenstackName string `json:"openstack_name,omitempty"`
	// Fingerprint holds the value of the "fingerprint" field.
	Fingerprint string `json:"fingerprint,omitempty"`
	// PublicKey holds the value of the "public_key" field.
	PublicKey string `json:"public_key,omitempty"`
	// SourceType holds the value of the "source_type" field.
	SourceType keypair.SourceType `json:"source_type,omitempty"`
	// SyncState holds the value of the "sync_state" field.
	SyncState keypair.SyncState `json:"sync_state,omitempty"`
	// LastSyncedAt holds the value of the "last_synced_at" field.
	LastSyncedAt time.Time `json:"last_synced_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the KeyPairQuery when eager-loading is set.
	Edges        KeyPairEdges `json:"edges"`
	selectValues sql.SelectValues
}

// KeyPairEdges holds the relations/edges for other nodes in the graph.
type KeyPairEdges struct {
	// Owner holds the value of the owner edge.
	Owner []*User `json:"owner,omitempty"`
	// Instances holds the value of the instances edge.
	Instances []*Instance `json:"instances,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading.
func (e KeyPairEdges) OwnerOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.Owner, nil
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// InstancesOrErr returns the Instances value or an error if the edge
// was not loaded in eager-loading.
func (e KeyPairEdges) InstancesOrErr() ([]*Instance, error) {
	if e.loadedTypes[1] {
		return e.Instances, nil
	}
	return nil, &NotLoadedError{edge: "instances"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*KeyPair) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case keypair.FieldOpenstackName, keypair.FieldFingerprint, keypair.FieldPublicKey, keypair.FieldSourceType, keypair.FieldSyncState:
			values[i] = new(sql.NullString)
		case keypair.FieldLastSyncedAt, keypair.FieldCreatedAt, keypair.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case keypair.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the KeyPair fields.
func (_m *KeyPair) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case keypair.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case keypair.FieldOpenstackName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field openstack_name", values[i])
			} else if value.Valid {
				_m.OpenstackName = value.String
			}
		case keypair.FieldFingerprint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field fingerprint", values[i])
			} else if value.Valid {
				_m.Fingerprint = value.String
			}
		case keypair.FieldPublicKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field public_key", values[i])
			} else if value.Valid {
				_m.PublicKey = value.String
			}
		case keypair.FieldSourceType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source_type", values[i])
			} else if value.Valid {
				_m.SourceType = keypair.SourceType(value.String)
			}
		case keypair.FieldSyncState:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sync_state", values[i])
			} else if value.Valid {
				_m.SyncState = keypair.SyncState(value.String)
			}
		case keypair.FieldLastSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_synced_at", values[i])
			} else if value.Valid {
				_m.LastSyncedAt = value.Time
			}
		case keypair.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case keypair.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the KeyPair.
// This includes values selected through modifiers, order, etc.
func (_m *KeyPair) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the KeyPair entity.
func (_m *KeyPair) QueryOwner() *UserQuery {
	return NewKeyPairClient(_m.config).QueryOwner(_m)
}

// QueryInstances queries the "instances" edge of the KeyPair entity.
func (_m *KeyPair) QueryInstances() *InstanceQuery {
	return NewKeyPairClient(_m.config).QueryInstances(_m)
}

// Update returns a builder for updating this KeyPair.
// Note that you need to call KeyPair.Unwrap() before calling this method if this KeyPair
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *KeyPair) Update() *KeyPairUpdateOne {
	return NewKeyPairClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the KeyPair entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *KeyPair) Unwrap() *KeyPair {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: KeyPair is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *KeyPair) String() string {
	var builder strings.Builder
	builder.WriteString("KeyPair(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("openstack_name=")
	builder.WriteString(_m.OpenstackName)
	builder.WriteString(", ")
	builder.WriteString("fingerprint=")
	builder.WriteString(_m.Fingerprint)
	builder.WriteString(", ")
	builder.WriteString("public_key=")
	builder.WriteString(_m.PublicKey)
	builder.WriteString(", ")
	builder.WriteString("source_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.SourceType))
	builder.WriteString(", ")
	builder.WriteString("sync_state=")
	builder.WriteString(fmt.Sprintf("%v", _m.SyncState))
	builder.WriteString(", ")
	builder.WriteString("last_synced_at=")
	builder.WriteString(_m.LastSyncedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// KeyPairs is a parsable slice of KeyPair.
type KeyPairs []*KeyPair
